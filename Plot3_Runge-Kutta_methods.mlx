1、 利用plot3绘制下列参数方程描述的三维螺旋线，并在指定的坐标平面上同时绘制其三个方向的投影曲线：
(1) 主曲线绘制：对参数取不少于个等间距采样点；曲线为红色实线，线宽为 2；
(2) 坐标系范围设置：要求显式设置坐标轴范围，使得；
(3) 在同一幅图上，再绘制改螺旋线在以下指定坐标平面上的三个投影： 平面(底面)， 平面(后墙)， 平面(侧墙)。所有投影颜色为灰色（[0.85, 0.85, 0.85]）。

2、 （1）编写函数实现求解常微分方程初值问题的龙格库塔方法（输入和输出参考课件中欧拉方法的实现函数）.
（2）将其应用于Logistic人口增长模型（参考课件，需和精确解对比）。

第一题
t=linspace(0,10*pi,500);
x=cos(t);
y=sin(t);
z=0.2*t;
plot3(x,y,z,'r','LineWidth',2);
axis([-2,2,-2,2,-1,2*pi+1]);
hold on
a=-ones(500);
b=1+ones(500);
plot3(x,y,a,'Color',[0.85,0.85,0.85]);
plot3(b,y,z,'Color',[0.85,0.85,0.85]);
plot3(x,b,z,'Color',[0.85,0.85,0.85]);
hold off
第二题
function [t,y]=longer(f,tspan,c)
a=tspan(1);
b=tspan(2);
n=100;
t=linspace(a,b,n+1);
y=zeros(1,n+1);
y(1)=c;
h=(b-a)/n;
for k=1:n
    k1=f(t(k),y(k));
    k2=f(t(k)+h/2,y(k)+h/2*k1);
    k3=f(t(k)+h/2,y(k)+h/2*k2);
    k4=f(t(k)+h,y(k)+h*k3);
    y(k+1)=y(k)+h/6*(k1+2*k2+2*k3+k4);
end
end
第三题
f=@(t,y)(1-y/20).*y;
tspan=[0,8];
c=1;
[t,y]=longer(f,tspan,c);
plot(t,y,'b',t,(20*exp(t))./(exp(t)+19),'r--');
